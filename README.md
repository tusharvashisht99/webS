STEPS TO INITIALIZE GIT TO PROJECT
    git init
    add .gitignore
    git remote add origin https url

STEPS TO PUSH CODE
    git add .
    git commit -m"initial and final commit"
    git push origin master --  branch name may vary